---
name: resource.install
description: Install MCP and workflow tooling for selected runtimes from resource state.
---

Read:

```bash
cat .vibe/resource/state.md
```

Require `[DETECT] confirmed: true`.

Use reference file:

`reference/resource.install.tools.md`

Install only selected runtimes and selected workflows.

Write status per item and set `[INSTALL] status: done` when completed.
